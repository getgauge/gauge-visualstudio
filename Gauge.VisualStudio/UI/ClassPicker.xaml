<platformUi:DialogWindow x:Class="Gauge.VisualStudio.UI.ClassPicker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.11.0"
        Title="ClassPicker" KeyDown="ClassPicker_OnKeyDown"
        Style="{DynamicResource ClassPickerStyle}" Height="80" Width="200" MaxHeight="300" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Theme/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid FocusManager.FocusedElement="{Binding ElementName=ClassListBox}" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0">Select Class</Label>
        <ComboBox Grid.Row="1" VerticalAlignment="Top" Width="190" x:Name="ClassListBox" ItemsSource="{Binding}" IsEditable="True" IsTextSearchEnabled="True"></ComboBox>
    </Grid>
</platformUi:DialogWindow>